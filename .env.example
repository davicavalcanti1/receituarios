# Supabase (frontend usa a publishable/anon key)
VITE_SUPABASE_URL=
VITE_SUPABASE_PUBLISHABLE_KEY=

# O frontend não precisa de nenhuma variável do NetRis: a integração é opcional
# e resolvida em runtime pelo servidor (GET /api/config). Ver server/.env.example.
